---
kind: ConfigMap
apiVersion: v1
metadata:
  name: properties
  namespace: {{ .Values.namespace | default "elysium" }}
  labels:
  {{- include "extw-integ.labels" . | nindent 4 }}
data:
  application.properties: |-
    {{- .Files.Get "application.properties" | nindent 4 }}
#  application-stg.properties: |-
#    {{- .Files.Get "application-stg.properties" | nindent 4 }}
#  application-prd.properties: |-
#    {{- .Files.Get "application-prd.properties" | nindent 4 }}
